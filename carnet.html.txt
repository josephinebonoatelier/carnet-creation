<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>Joséphine Bono — Carnet de création</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;1,300;1,400&family=Jost:wght@300;400;500&display=swap" rel="stylesheet">
<style>
*{box-sizing:border-box;margin:0;padding:0}
:root{--cr:#FAF8F5;--iv:#F5F0E8;--go:#C4A66A;--gl:#E8D9B8;--tx:#2C2620;--mu:#8C7B6B;--br:rgba(196,166,106,.2);--bs:rgba(196,166,106,.45)}
body{font-family:'Jost',sans-serif;background:var(--cr);color:var(--tx);min-height:100vh;-webkit-font-smoothing:antialiased}

/* ── LOGIN ── */
#pg-login{position:fixed;top:0;left:0;width:100%;height:100%;display:flex;align-items:center;justify-content:center;background:var(--iv);z-index:9999;overflow-y:auto}
.lc{text-align:center;width:380px;max-width:94vw;padding:52px 44px;background:white;border:.5px solid var(--br)}
.lc-logo{font-family:'Cormorant Garamond',serif;font-size:28px;font-weight:300;letter-spacing:.15em;color:var(--tx)}
.lc-sub{font-size:9px;letter-spacing:.35em;text-transform:uppercase;color:var(--go);margin:3px 0 6px}
.lc-hint{font-size:12px;color:var(--mu);font-style:italic;margin-bottom:28px;line-height:1.5}
.lc input{width:100%;border:.5px solid var(--bs);background:var(--cr);padding:14px;font-family:'Jost',sans-serif;font-size:16px;color:var(--tx);text-align:center;letter-spacing:.15em;outline:none;margin-bottom:12px;text-transform:uppercase}
.lc input:focus{border-color:var(--go)}
.lc-btn{width:100%;background:var(--tx);color:var(--gl);font-family:'Jost',sans-serif;font-size:10px;letter-spacing:.28em;text-transform:uppercase;padding:14px;border:none;cursor:pointer}
.lc-err{color:#c0392b;font-size:12px;margin-top:10px;display:none}

/* ── PAGES ── */
.page{display:none;min-height:100vh}
.page.active{display:block}

/* ── HEADER COMMUN ── */
.app-header{background:white;border-bottom:.5px solid var(--br);padding:0 32px;display:flex;align-items:center;justify-content:space-between;height:58px;position:sticky;top:0;z-index:100}
.app-header-brand{font-family:'Cormorant Garamond',serif;font-size:18px;font-weight:300;letter-spacing:.12em;color:var(--tx)}
.app-header-sub{font-size:8px;letter-spacing:.28em;text-transform:uppercase;color:var(--go);margin-top:2px}
.app-nav{display:flex;gap:0}
.nav-btn{font-family:'Jost',sans-serif;font-size:9px;letter-spacing:.14em;text-transform:uppercase;color:var(--mu);padding:8px 14px;cursor:pointer;border:none;background:none;border-bottom:2px solid transparent;transition:all .15s}
.nav-btn:hover{color:var(--tx)}
.nav-btn.active{color:var(--tx);border-bottom-color:var(--go)}
.nav-btn.bo{color:var(--go)}
.nav-btn.bo.active{color:var(--tx)}

/* ── HERO CLIENTE ── */
.hero{background:white;border-bottom:.5px solid var(--br);padding:36px 24px 28px;text-align:center}
.hero-eyebrow{font-size:9px;letter-spacing:.38em;text-transform:uppercase;color:var(--go);margin-bottom:12px}
.hero-name{font-family:'Cormorant Garamond',serif;font-size:38px;font-weight:300;color:var(--tx);line-height:1.1}
.hero-dates{display:flex;gap:24px;justify-content:center;margin-top:8px;flex-wrap:wrap}
.hero-date-pill{font-size:11px;color:var(--mu)}
.hero-date-pill strong{color:var(--tx);font-weight:500}
.hero-sep{width:28px;height:.5px;background:var(--go);margin:14px auto}
.countdown{display:flex;gap:24px;justify-content:center;margin:8px 0}
.ci-num{font-family:'Cormorant Garamond',serif;font-size:34px;font-weight:300;color:var(--tx);line-height:1}
.ci-unit{font-size:9px;letter-spacing:.18em;text-transform:uppercase;color:var(--mu);margin-top:3px}
.hero-model{font-family:'Cormorant Garamond',serif;font-size:16px;font-style:italic;color:var(--go)}
.hero-sig{font-size:12px;font-style:italic;color:var(--mu);margin-top:5px}

/* ── SECTIONS CLIENTE ── */
.cl-sec{display:none;max-width:820px;margin:0 auto;padding:28px 20px 60px}
.cl-sec.active{display:block}

/* ── ADMIN HEADER ── */
.bo-header{background:var(--tx);padding:20px 36px;display:flex;align-items:center;justify-content:space-between}
.bo-header-title{font-family:'Cormorant Garamond',serif;font-size:22px;color:var(--gl);letter-spacing:.06em}
.bo-header-sub{font-size:9px;letter-spacing:.25em;text-transform:uppercase;color:var(--go);margin-top:3px}
.bo-header-bride{font-family:'Cormorant Garamond',serif;font-size:18px;color:var(--go);text-align:right}
.bo-header-code{font-size:9px;letter-spacing:.2em;text-transform:uppercase;color:rgba(232,217,184,.5);margin-top:2px;text-align:right}
.bo-tabs-wrap{background:white;border-bottom:.5px solid var(--br);overflow-x:auto;-webkit-overflow-scrolling:touch}
.bo-tabs{display:flex;min-width:max-content;padding:0 24px}
.bo-tab{font-family:'Jost',sans-serif;font-size:10px;letter-spacing:.14em;text-transform:uppercase;color:var(--mu);padding:14px 18px;cursor:pointer;border:none;background:none;border-bottom:2px solid transparent;margin-bottom:-1px;white-space:nowrap;transition:all .15s}
.bo-tab:hover{color:var(--tx);background:var(--iv)}
.bo-tab.active{color:var(--tx);border-bottom-color:var(--go);font-weight:500}

/* ── ADMIN SECTIONS ── */
.bo-sec{display:none;padding:32px 36px;max-width:1000px}
.bo-sec.active{display:block}

/* ── COMPOSANTS GÉNÉRAUX ── */
.sl{font-family:'Cormorant Garamond',serif;font-size:20px;color:var(--tx);margin:24px 0 14px;display:flex;align-items:center;gap:10px}
.sl::after{content:'';flex:1;height:.5px;background:var(--br)}
.sl:first-child{margin-top:0}

.card{background:white;border:.5px solid var(--br);padding:20px 22px;margin-bottom:14px}
.card-title{font-size:9px;letter-spacing:.22em;text-transform:uppercase;color:var(--go);margin-bottom:14px;padding-bottom:8px;border-bottom:.5px solid var(--br)}

.fg{display:grid;gap:14px 18px}
.fg-2{grid-template-columns:1fr 1fr}
.fg-3{grid-template-columns:1fr 1fr 1fr}
.fg-4{grid-template-columns:1fr 1fr 1fr 1fr}

.field{display:flex;flex-direction:column;gap:5px}
.field label{font-size:9px;letter-spacing:.2em;text-transform:uppercase;color:var(--mu)}
.field input,.field select,.field textarea{font-family:'Jost',sans-serif;font-size:13px;color:var(--tx);background:white;border:.5px solid var(--bs);padding:10px 12px;outline:none}
.field input:focus,.field select:focus,.field textarea:focus{border-color:var(--go)}
.field textarea{resize:vertical;min-height:80px}

.btn-dark{background:var(--tx);color:var(--gl);font-family:'Jost',sans-serif;font-size:10px;letter-spacing:.18em;text-transform:uppercase;padding:11px 22px;border:none;cursor:pointer}
.btn-dark:hover{background:#3d3530}
.btn-outline{background:transparent;color:var(--mu);border:.5px solid var(--bs);font-family:'Jost',sans-serif;font-size:10px;letter-spacing:.14em;text-transform:uppercase;padding:10px 18px;cursor:pointer}
.btn-gold{background:rgba(196,166,106,.1);color:#8C6A2F;border:.5px solid rgba(196,166,106,.4);font-family:'Jost',sans-serif;font-size:10px;letter-spacing:.14em;text-transform:uppercase;padding:10px 18px;cursor:pointer}
.btn-sm{padding:7px 12px;font-size:9px}
.btn-danger{background:transparent;color:#c0392b;border:.5px solid rgba(192,57,43,.2);font-family:'Jost',sans-serif;font-size:9px;padding:5px 9px;cursor:pointer}
.action-row{display:flex;gap:10px;margin-top:14px;flex-wrap:wrap;align-items:center}

/* ── PLANNING ── */
.plan-list{display:flex;flex-direction:column}
.plan-item{display:flex;align-items:flex-start;gap:12px;padding:13px 0;border-bottom:.5px solid rgba(196,166,106,.1)}
.plan-dot{width:10px;height:10px;border-radius:50%;flex-shrink:0;margin-top:4px}
.plan-dot.done{background:var(--go)}.plan-dot.next{background:transparent;border:2px solid var(--go)}.plan-dot.future{background:transparent;border:1.5px solid var(--br)}
.plan-title{font-size:14px;color:var(--tx)}.plan-date{font-size:11px;color:var(--mu);margin-top:3px}
.plan-badge{font-size:10px;padding:3px 10px;margin-left:auto;flex-shrink:0}
.pb-done{background:rgba(196,166,106,.1);color:#8C6A2F;border:.5px solid rgba(196,166,106,.3)}
.pb-next{background:var(--tx);color:var(--gl)}.pb-future{color:var(--mu)}
.plan-edit-row{display:grid;grid-template-columns:2.5fr 1.2fr 1fr auto;gap:10px;align-items:center;padding:10px 12px;background:var(--iv);border:.5px solid var(--br);margin-bottom:8px}
.plan-edit-row input,.plan-edit-row select{border:.5px solid var(--bs);padding:8px 10px;font-family:'Jost',sans-serif;font-size:12px;color:var(--tx);background:white;outline:none;width:100%}

/* ── MESURES ── */
.mes-group{margin-bottom:20px}
.mes-group-title{font-size:9px;letter-spacing:.22em;text-transform:uppercase;color:var(--go);margin-bottom:10px;padding-bottom:6px;border-bottom:.5px solid var(--br)}
.mes-grid-view{display:grid;grid-template-columns:repeat(auto-fill,minmax(140px,1fr));gap:8px}
.mes-card{background:white;border:.5px solid var(--br);padding:10px 12px}
.mes-lbl{font-size:9px;letter-spacing:.1em;text-transform:uppercase;color:var(--mu);margin-bottom:4px;line-height:1.3}
.mes-val{font-family:'Cormorant Garamond',serif;font-size:20px;color:var(--tx)}
.mes-notes-box{background:white;border:.5px solid var(--br);padding:14px;font-size:13px;color:var(--tx);line-height:1.7;margin-top:10px}
.mes-grid-form{display:grid;grid-template-columns:repeat(auto-fill,minmax(160px,1fr));gap:10px 14px}

/* ── ESSAYAGES ── */
.essa-item{background:white;border:.5px solid var(--br);margin-bottom:12px;overflow:hidden}
.essa-hd{padding:12px 16px;border-bottom:.5px solid var(--br);display:flex;align-items:center;justify-content:space-between}
.essa-hd-left{display:flex;align-items:center;gap:12px}
.essa-date-lbl{font-size:10px;letter-spacing:.12em;text-transform:uppercase;color:var(--go)}
.essa-num{font-family:'Cormorant Garamond',serif;font-size:16px;color:var(--tx)}
.essa-body{padding:13px 16px;font-size:13px;color:var(--tx);line-height:1.7}
.essa-photos{display:flex;gap:8px;padding:0 16px 12px;flex-wrap:wrap}
.essa-photo{width:100px;height:100px;object-fit:cover;border:.5px solid var(--br)}

/* ── JOURNAL ── */
.journal-item{background:white;border:.5px solid var(--br);margin-bottom:16px;overflow:hidden}
.journal-img{width:100%;max-height:420px;object-fit:cover;display:block}
.journal-body{padding:16px 18px}
.journal-meta{font-size:10px;letter-spacing:.12em;text-transform:uppercase;color:var(--go);margin-bottom:6px}
.journal-title{font-family:'Cormorant Garamond',serif;font-size:22px;color:var(--tx);margin-bottom:8px}
.journal-text{font-size:13px;color:var(--tx);line-height:1.8}

/* ── CHECKLIST ── */
.check-cat{margin-bottom:22px}
.check-cat-title{font-size:9px;letter-spacing:.2em;text-transform:uppercase;color:var(--go);margin-bottom:9px;padding-bottom:6px;border-bottom:.5px solid var(--br)}
.check-cat-title.jb-advice{font-size:10px;letter-spacing:.16em;color:white;background:var(--tx);padding:10px 14px;margin-bottom:0;border:none;font-family:'Cormorant Garamond',serif;font-style:italic;font-size:14px;text-transform:none;letter-spacing:.04em}
.check-item.jb-advice-item{background:rgba(196,166,106,.06);border-color:rgba(196,166,106,.3)}
.check-item.jb-advice-item .check-box{border-color:var(--go)}
.check-item.jb-advice-item.done .check-box{background:var(--go);border-color:var(--go)}
.check-list{display:flex;flex-direction:column;gap:6px}
.check-item{display:flex;align-items:center;gap:11px;padding:11px 13px;background:white;border:.5px solid var(--br);cursor:pointer}
.check-item.done{opacity:.5}
.check-box{width:17px;height:17px;border:.5px solid var(--bs);flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:10px;color:transparent}
.check-item.done .check-box{background:var(--go);border-color:var(--go);color:white}
.check-name{flex:1;font-size:13px;color:var(--tx)}
.check-item.done .check-name{text-decoration:line-through;color:var(--mu)}
.check-delay{font-family:'Cormorant Garamond',serif;font-size:14px;color:var(--go);flex-shrink:0}

/* ── PAIEMENTS ── */
.pay-table{border:.5px solid var(--br);overflow:hidden;margin-bottom:14px}
.pay-row{display:grid;grid-template-columns:1.2fr 1.5fr 1fr 1fr;gap:10px;padding:11px 14px;border-bottom:.5px solid rgba(196,166,106,.08);align-items:center;font-size:13px}
.pay-row.head{background:var(--iv);font-size:9px;letter-spacing:.15em;text-transform:uppercase;color:var(--mu);padding:9px 14px}
.pay-amount{font-family:'Cormorant Garamond',serif;font-size:16px}
.badge{font-size:10px;padding:4px 10px;display:inline-block}
.b-paid{background:rgba(60,130,70,.08);color:#2E6E35;border:.5px solid rgba(60,130,70,.2)}
.b-pend{background:rgba(196,166,106,.12);color:#8C6A2F;border:.5px solid rgba(196,166,106,.35)}
.b-fut{background:rgba(140,123,107,.07);color:var(--mu);border:.5px solid var(--br)}
.pay-info{background:var(--iv);border:.5px solid var(--br);padding:13px 14px;font-size:12px;color:var(--mu);line-height:1.8;margin-top:10px}

/* ── DOCUMENTS ── */
.doc-item{display:flex;align-items:center;gap:12px;padding:13px 16px;background:white;border:.5px solid var(--br);margin-bottom:8px}
.doc-icon{width:36px;height:36px;background:var(--iv);border:.5px solid var(--br);display:flex;align-items:center;justify-content:center;font-size:16px;flex-shrink:0}
.doc-name{flex:1;font-size:13px;color:var(--tx);font-weight:500}
.doc-type{font-size:11px;color:var(--mu);margin-top:2px}
.doc-dl{background:var(--tx);color:var(--gl);font-family:'Jost',sans-serif;font-size:9px;letter-spacing:.15em;text-transform:uppercase;padding:8px 14px;border:none;cursor:pointer;text-decoration:none;display:inline-block}

/* ── RDV ── */
.rdv-wrap{text-align:center;padding:40px 20px}
.rdv-title{font-family:'Cormorant Garamond',serif;font-size:28px;margin-bottom:8px}
.rdv-sub{font-size:13px;color:var(--mu);margin-bottom:26px;line-height:1.6}
.rdv-btns{display:flex;gap:12px;justify-content:center;flex-wrap:wrap}
.rdv-btn{font-family:'Jost',sans-serif;font-size:10px;letter-spacing:.18em;text-transform:uppercase;padding:13px 24px;text-decoration:none;display:inline-block}
.rdv-dark{background:var(--tx);color:var(--gl);border:none}
.rdv-wa{background:#25D366;color:white;border:none}
.rdv-contact{margin-top:24px;font-size:13px;color:var(--mu);line-height:1.9}

/* ── FOOTER ── */
.jb-footer{border-top:.5px solid var(--br);padding:20px 24px;text-align:center;margin-top:40px}
.footer-brand{font-family:'Cormorant Garamond',serif;font-size:15px;color:var(--tx);letter-spacing:.15em}
.footer-sub{font-size:9px;letter-spacing:.2em;text-transform:uppercase;color:var(--go);margin-top:4px;opacity:.7}

/* ── TOAST ── */
#jb-toast{position:fixed;bottom:18px;right:18px;z-index:999;display:flex;flex-direction:column;gap:6px}
.toast-item{background:var(--tx);color:var(--gl);padding:10px 16px;font-size:12px;border-left:3px solid var(--go);animation:tin .2s ease}
@keyframes tin{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}

/* ── JOURNAL PRINT / LIVRET ── */
@media print{
  .app-header,.bo-header,.bo-tabs-wrap,.nav-btn,.btn-dark,.btn-outline,.btn-gold,.btn-danger,.jb-footer,#jb-toast,.action-row,button{display:none!important}
  body{background:white}
  .page,.cl-sec,.bo-sec{display:block!important}
  .journal-item{break-inside:avoid;page-break-inside:avoid;border:none;margin-bottom:32px}
  .journal-img{max-height:300px}
  @page{margin:20mm}
}

@media(max-width:640px){
  .fg-3,.fg-4{grid-template-columns:1fr 1fr}
  .mes-grid-form{grid-template-columns:1fr 1fr}
  .pay-row{grid-template-columns:1fr 1fr;font-size:11px}
  .plan-edit-row{grid-template-columns:1fr 1fr;gap:6px}
  .bo-sec{padding:20px 16px}
  .app-header{padding:0 16px}
}
@media(max-width:400px){.fg-2{grid-template-columns:1fr}}
</style>
</head>
<body>

<!-- LOGIN -->
<div id="pg-login" class="page active">
  <div class="lc">
    <!-- VUE CLIENTE -->
    <div id="lc-cliente">
      <div class="lc-logo">Joséphine Bono</div>
      <div class="lc-sub">Carnet de création</div>
      <p class="lc-hint">Entrez votre code d'accès<br>inscrit sur votre devis</p>
      <input type="text" id="jb-code" placeholder="Ex : CAMILLE2009" maxlength="14" autocomplete="off" onkeydown="if(event.key==='Enter')jbLogin()">
      <button class="lc-btn" id="lc-btn-client" onclick="jbLogin()">Accéder à mon carnet →</button>
      <div class="lc-err" id="jb-err">Code introuvable — vérifiez votre devis</div>
      <div style="margin-top:28px;padding-top:18px;border-top:.5px solid var(--br)">
        <button onclick="jbShowAdminLogin()" style="background:none;border:none;font-family:'Jost',sans-serif;font-size:10px;letter-spacing:.16em;text-transform:uppercase;color:var(--go);cursor:pointer">✦ Accès atelier</button>
      </div>
    </div>
    <!-- VUE ATELIER -->
    <div id="lc-atelier" style="display:none">
      <div class="lc-logo">Joséphine Bono</div>
      <div class="lc-sub" style="color:var(--tx)">Administration atelier</div>
      <p class="lc-hint">Entrez votre mot de passe atelier</p>
      <input type="password" id="jb-pwd-bo" placeholder="Mot de passe atelier" autocomplete="off" onkeydown="if(event.key==='Enter')jbLoginBO()">
      <button class="lc-btn" id="lc-btn-bo" onclick="jbLoginBO()">Accéder au back-office →</button>
      <div class="lc-err" id="jb-err-bo">Mot de passe incorrect</div>
      <div style="margin-top:18px">
        <button onclick="jbShowClientLogin()" style="background:none;border:none;font-family:'Jost',sans-serif;font-size:10px;letter-spacing:.16em;text-transform:uppercase;color:var(--mu);cursor:pointer">← Retour</button>
      </div>
    </div>
  </div>
</div>

<!-- PAGE CLIENTE -->
<div id="pg-client" class="page">
  <div class="app-header">
    <div>
      <div class="app-header-brand">Joséphine Bono</div>
      <div class="app-header-sub">Votre carnet de création</div>
    </div>
    <nav class="app-nav" id="cl-nav">
      <button class="nav-btn active" onclick="clSection('planning',this)">Planning</button>
      <button class="nav-btn" onclick="clSection('essayages',this)">Essayages &amp; Mesures</button>
      <button class="nav-btn" onclick="clSection('journal',this)">Journal atelier</button>
      <button class="nav-btn" onclick="clSection('preparatifs',this)">Préparatifs</button>
      <button class="nav-btn" onclick="clSection('paiements',this)">Paiements &amp; Documents</button>
      <button class="nav-btn" onclick="clSection('rdv',this)">Rendez-vous</button>
    </nav>
  </div>

  <div class="hero">
    <div class="hero-eyebrow">Votre carnet de création</div>
    <div class="hero-name" id="cl-name">—</div>
    <div class="hero-dates" id="cl-dates"></div>
    <div class="hero-sep"></div>
    <div class="countdown" id="cl-count"></div>
    <div class="hero-model" id="cl-model"></div>
    <div class="hero-sig" id="cl-sig"></div>
  </div>

  <div class="cl-sec active" id="cl-sec-planning">
    <div class="sl">Votre parcours de création</div>
    <div class="plan-list" id="cl-planning"></div>
  </div>

  <div class="cl-sec" id="cl-sec-essayages">
    <div id="cl-mesures"></div>
    <div class="sl" style="margin-top:28px">Comptes-rendus des essayages</div>
    <div id="cl-essayages"></div>
    <p id="cl-essa-empty" style="font-size:14px;color:var(--mu);font-style:italic;padding:10px 0;display:none">Les comptes-rendus seront ajoutés par l'atelier après chaque essayage.</p>
  </div>

  <div class="cl-sec" id="cl-sec-journal">
    <div class="sl">Le fil de votre création</div>
    <p style="font-size:13px;color:var(--mu);margin-bottom:22px;font-style:italic">Joséphine partage ici les étapes de fabrication de votre robe, semaine après semaine.</p>
    <div id="cl-journal"></div>
    <p id="cl-journal-empty" style="text-align:center;padding:36px 0;font-family:'Cormorant Garamond',serif;font-size:20px;color:var(--mu);display:none">Le journal sera alimenté au fil de la création</p>
  </div>

  <div class="cl-sec" id="cl-sec-preparatifs">
    <div class="sl">Votre checklist mariage</div>
    <p style="font-size:13px;color:var(--mu);margin-bottom:18px;font-style:italic">Cochez au fur et à mesure — votre progression est sauvegardée.</p>
    <div id="cl-checklist"></div>
  </div>

  <div class="cl-sec" id="cl-sec-paiements">
    <div class="sl">Suivi des paiements</div>
    <div class="pay-table">
      <div class="pay-row head"><span>Type</span><span>Description</span><span>Montant HT</span><span>Statut</span></div>
      <div id="cl-pay-rows"></div>
    </div>
    <div id="cl-pay-btn" style="display:none;margin-top:14px">
      <button class="btn-dark" onclick="window.open('https://buy.stripe.com/cNi14gfeRabUdwb8BxaEE00','_blank')">Payer en ligne — Stripe</button>
    </div>
    <div class="pay-info">Virement bancaire · Espèces · Carte bancaire · PayPal<br>Chaque mensualité doit être réglée au plus tard la veille du rendez-vous d'essayage correspondant.<br>Le solde intégral doit être réglé avant la livraison.</div>
    <div class="sl" style="margin-top:28px">Vos documents</div>
    <div id="cl-docs"></div>
    <p id="cl-docs-empty" style="font-size:14px;color:var(--mu);font-style:italic;padding:8px 0">Vos devis et factures seront disponibles ici dès qu'ils auront été transmis par l'atelier.</p>
  </div>

  <div class="cl-sec" id="cl-sec-rdv">
    <div class="rdv-wrap">
      <div class="rdv-title">Prendre rendez-vous</div>
      <p class="rdv-sub">Réservez votre prochain essayage en ligne<br>ou contactez-moi directement.</p>
      <div class="rdv-btns">
        <a class="rdv-btn rdv-dark" href="https://calendly.com/josephine-bono-rendez-vous" target="_blank">Calendly — Réserver en ligne</a>
        <a class="rdv-btn rdv-wa" href="https://wa.me/33698942000" target="_blank">WhatsApp</a>
      </div>
      <div class="rdv-contact"><strong>Joséphine Bono</strong><br>26 avenue de la Gare · 33470 Gujan-Mestras<br>contact@josephinebono.com · +33 6 98 94 20 00</div>
    </div>
  </div>

  <div class="jb-footer"><div class="footer-brand">Joséphine Bono</div><div class="footer-sub">Atelier couture bridal · Bassin d'Arcachon</div></div>
</div>

<!-- PAGE BACK-OFFICE ATELIER -->
<div id="pg-admin" class="page">
  <div class="app-header" style="background:var(--tx);border-bottom:.5px solid rgba(196,166,106,.3)">
    <div>
      <div class="app-header-brand" style="color:var(--gl)">Joséphine Bono</div>
      <div class="app-header-sub">Administration atelier</div>
    </div>
    <nav class="app-nav">
      <button class="nav-btn bo active" onclick="boSection('infos',this)" style="color:rgba(232,217,184,.6)">Infos</button>
      <button class="nav-btn bo" onclick="boSection('planning',this)" style="color:rgba(232,217,184,.6)">Planning</button>
      <button class="nav-btn bo" onclick="boSection('mesures',this)" style="color:rgba(232,217,184,.6)">Mesures</button>
      <button class="nav-btn bo" onclick="boSection('essayages',this)" style="color:rgba(232,217,184,.6)">Essayages</button>
      <button class="nav-btn bo" onclick="boSection('journal',this)" style="color:rgba(232,217,184,.6)">Journal</button>
      <button class="nav-btn bo" onclick="boSection('paiements',this)" style="color:rgba(232,217,184,.6)">Paiements</button>
      <button class="nav-btn bo" onclick="boSection('documents',this)" style="color:rgba(232,217,184,.6)">Documents</button>
      <button class="nav-btn bo" onclick="switchToClient()" style="color:var(--go)">→ Vue cliente</button>
    </nav>
  </div>
  <div id="bo-bride-bar" style="background:rgba(196,166,106,.1);border-bottom:.5px solid var(--br);padding:9px 36px;font-size:12px;color:var(--mu);display:flex;align-items:center;justify-content:space-between">
    <span id="bo-bride-label">Carnet de <strong>—</strong></span>
    <span id="bo-code-label" style="font-family:'Cormorant Garamond',serif;font-size:16px;color:var(--go)"></span>
  </div>

  <!-- BO INFOS -->
  <div class="bo-sec active" id="bo-sec-infos">
    <div class="sl">Informations générales</div>
    <div class="card">
      <div class="card-title">Identité & création</div>
      <div class="fg fg-3">
        <div class="field"><label>Prénom de la mariée</label><input type="text" id="b-nom" placeholder="Ex : Camille"></div>
        <div class="field"><label>Modèle / titre de la création</label><input type="text" id="b-modele" placeholder="Ex : Soleil d'Arcachon"></div>
        <div class="field"><label>Phrase personnalisée</label><input type="text" id="b-sig" placeholder="Message pour la mariée..."></div>
        <div class="field"><label>Date du mariage</label><input type="date" id="b-mariage"></div>
        <div class="field"><label>Date de livraison</label><input type="date" id="b-livraison"></div>
      </div>
      <div class="action-row">
        <button class="btn-dark" onclick="jbSaveInfos()">Enregistrer</button>
        <span id="infos-saved" style="font-size:12px;color:var(--go);display:none">✓ Enregistré</span>
      </div>
    </div>
  </div>

  <!-- BO PLANNING -->
  <div class="bo-sec" id="bo-sec-planning">
    <div class="sl">Planning des rendez-vous</div>
    <div class="card">
      <div class="card-title">Étapes de création</div>
      <div id="bo-plan-rows"></div>
      <button class="btn-outline btn-sm" onclick="jbAddPlanRow()" style="margin-top:8px">+ Ajouter une étape</button>
      <div class="action-row">
        <button class="btn-dark" onclick="jbSavePlanning()">Enregistrer le planning</button>
        <span id="plan-saved" style="font-size:12px;color:var(--go);display:none">✓ Enregistré</span>
      </div>
    </div>
  </div>

  <!-- BO MESURES -->
  <div class="bo-sec" id="bo-sec-mesures">
    <div class="sl">Mesures au corps</div>
    <div id="bo-mes-groups"></div>
    <div class="card" style="margin-top:14px">
      <div class="card-title">Notes mesures</div>
      <div class="field"><textarea id="b-mes-notes" rows="4" placeholder="Observations, particularités morphologiques..."></textarea></div>
    </div>
    <div class="action-row">
      <button class="btn-dark" onclick="jbSaveMesures()">Enregistrer toutes les mesures</button>
      <span id="mes-saved" style="font-size:12px;color:var(--go);display:none">✓ Enregistré</span>
    </div>
  </div>

  <!-- BO ESSAYAGES -->
  <div class="bo-sec" id="bo-sec-essayages">
    <div class="sl">Comptes-rendus d'essayages</div>
    <div class="card">
      <div class="card-title">Ajouter un essayage</div>
      <div class="fg fg-2" style="margin-bottom:12px">
        <div class="field"><label>Date</label><input type="date" id="b-ess-date"></div>
        <div class="field"><label>Libellé</label><input type="text" id="b-ess-num" placeholder="Ex : 1er essayage — toile"></div>
      </div>
      <div class="field" style="margin-bottom:12px"><label>Compte-rendu & observations</label><textarea id="b-ess-notes" rows="5" placeholder="Ajustements réalisés, remarques morphologiques, éléments à retravailler..."></textarea></div>
      <div class="field" style="margin-bottom:12px"><label>URL photo (copier le lien depuis Supabase Storage)</label><input type="url" id="b-ess-photo" placeholder="https://..."></div>
      <div class="action-row"><button class="btn-dark" onclick="jbSaveEssayage()">Enregistrer cet essayage</button></div>
    </div>
    <div class="sl" style="margin-top:24px">Essayages enregistrés</div>
    <div id="bo-essa-list"></div>
    <p id="bo-essa-empty" style="font-size:13px;color:var(--mu);font-style:italic">Aucun essayage enregistré pour le moment.</p>
  </div>

  <!-- BO JOURNAL -->
  <div class="bo-sec" id="bo-sec-journal">
    <div class="sl">Journal de création</div>
    <div class="card">
      <div class="card-title">Publier une nouvelle entrée</div>
      <div class="fg fg-2" style="margin-bottom:12px">
        <div class="field"><label>Date</label><input type="date" id="b-jrn-date"></div>
        <div class="field"><label>Titre de l'étape</label><input type="text" id="b-jrn-titre" placeholder="Ex : Découpe du satin duchesse"></div>
      </div>
      <div class="field" style="margin-bottom:12px"><label>Récit de l'étape — ce que verra la mariée</label><textarea id="b-jrn-texte" rows="6" placeholder="Décrivez cette étape avec soin — la mariée vivra la création de sa robe à travers votre récit..."></textarea></div>
      <div class="field" style="margin-bottom:12px"><label>URL photo</label><input type="url" id="b-jrn-photo" placeholder="https://..."></div>
      <div class="action-row"><button class="btn-dark" onclick="jbSaveJournal()">Publier cette entrée</button></div>
    </div>
    <div class="sl" style="margin-top:24px">Entrées publiées</div>
    <div id="bo-journal-list"></div>
    <div style="margin-top:20px;display:flex;gap:12px">
      <button class="btn-gold" onclick="window.print()">Imprimer le livret souvenir (PDF)</button>
      <span style="font-size:12px;color:var(--mu);align-self:center">→ Imprimez ou exportez en PDF depuis votre navigateur</span>
    </div>
  </div>

  <!-- BO PAIEMENTS -->
  <div class="bo-sec" id="bo-sec-paiements">
    <div class="sl">Suivi des paiements</div>
    <div class="card">
      <div class="card-title">Mise à jour des statuts</div>
      <div id="bo-pay-rows"></div>
      <div class="action-row">
        <button class="btn-dark" onclick="jbSavePaiements()">Enregistrer les statuts</button>
        <span id="pay-saved" style="font-size:12px;color:var(--go);display:none">✓ Enregistré</span>
      </div>
    </div>
  </div>

  <!-- BO DOCUMENTS -->
  <div class="bo-sec" id="bo-sec-documents">
    <div class="sl">Documents — Devis &amp; Factures</div>
    <div class="card">
      <div class="card-title">Ajouter un document</div>
      <p style="font-size:12px;color:var(--mu);margin-bottom:14px;line-height:1.7">Collez l'URL de partage du PDF (depuis Google Drive, Dropbox ou autre) pour que la cliente puisse le télécharger depuis son carnet.</p>
      <div class="fg fg-3" style="margin-bottom:12px">
        <div class="field"><label>Nom du document</label><input type="text" id="b-doc-nom" placeholder="Ex : Devis DV-2026-001"></div>
        <div class="field"><label>Type</label>
          <select id="b-doc-type">
            <option value="devis">Devis</option>
            <option value="facture">Facture</option>
            <option value="autre">Autre</option>
          </select>
        </div>
        <div class="field"><label>URL du PDF (lien de partage)</label><input type="url" id="b-doc-url" placeholder="https://drive.google.com/..."></div>
      </div>
      <div class="action-row"><button class="btn-dark" onclick="jbSaveDoc()">Ajouter ce document</button></div>
    </div>
    <div class="sl" style="margin-top:24px">Documents disponibles</div>
    <div id="bo-docs-list"></div>
    <p id="bo-docs-empty" style="font-size:13px;color:var(--mu);font-style:italic">Aucun document ajouté.</p>
  </div>

  <div class="jb-footer"><div class="footer-brand">Joséphine Bono</div><div class="footer-sub">Administration atelier · Back-office</div></div>
</div>

<div id="jb-toast"></div>

<script>
var SU='https://yjnzydmytimqbusnitqr.supabase.co';
var SK='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inlqbnp5ZG15dGltcWJ1c25pdHFyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzQzODI4MDMsImV4cCI6MjA4OTk1ODgwM30.uuHWWlBtdML7NrnC1RPa2mS07GMtLfmlSTs13dfCgBQ';
var CARNET=null,DEVIS=null,IS_BO=false;

async function sf(t,m,b,q){
  var r=await fetch(SU+'/rest/v1/'+t+(q?'?'+q:''),(m=m||'GET',{method:m,headers:{'apikey':SK,'Authorization':'Bearer '+SK,'Content-Type':'application/json','Prefer':'return=representation'},body:b?JSON.stringify(b):undefined}));
  if(!r.ok)throw new Error(await r.text());
  var ct=r.headers.get('content-type')||'';
  if(ct.includes('json')&&r.status!==204)return await r.json();
  return null;
}
var sg=function(t,q){return sf(t,'GET',null,q)};
var sp=function(t,q,b){return sf(t,'PATCH',b,q)};

function jbShowAdminLogin(){
  document.getElementById('lc-cliente').style.display='none';
  document.getElementById('lc-atelier').style.display='block';
  document.getElementById('jb-pwd-bo').focus();
}
function jbShowClientLogin(){
  document.getElementById('lc-atelier').style.display='none';
  document.getElementById('lc-cliente').style.display='block';
}

async function jbLoginBO(){
  var pwd=document.getElementById('jb-pwd-bo').value.trim();
  if(!pwd)return;
  var errEl=document.getElementById('jb-err-bo');
  var btn=document.getElementById('lc-btn-bo');
  errEl.style.display='none';
  btn.textContent='Chargement...';btn.disabled=true;
  try{
    var cfg=null;
    try{
      var p=sg('jb_config','id=eq.singleton');
      var t=new Promise(function(_,rej){setTimeout(function(){rej(new Error('timeout'));},6000);});
      cfg=await Promise.race([p,t]);
    }catch(e){cfg=null;}
    var ap=(cfg&&cfg[0]&&cfg[0].password)?cfg[0].password.toUpperCase():'JB2025';
    if(pwd.toUpperCase()!==ap && pwd!=='jb2025'){
      errEl.style.display='block';
      return;
    }
    IS_BO=true;
    // Charger le carnet indiqué par ?code= dans l'URL
    var params=new URLSearchParams(window.location.search);
    var clientCode=params.get('code');
    if(!clientCode){
      showPage('pg-admin');
      jbToast('Back-office ouvert — ajoutez ?code=XXXXX dans l\'URL pour charger un carnet');
      return;
    }
    var data=await sg('jb_carnets','code_acces=eq.'+clientCode.toUpperCase());
    if(!data||!data.length){
      errEl.textContent='Carnet '+clientCode+' introuvable';
      errEl.style.display='block';
      return;
    }
    CARNET=data[0];
    if(CARNET.devis_id){
      try{var dd=await sg('jb_devis','id=eq.'+CARNET.devis_id);if(dd&&dd[0])DEVIS=dd[0];}catch(e){}
    }
    showPage('pg-admin');
    jbRenderAdmin();
  }catch(e){
    errEl.textContent='Erreur : '+e.message;
    errEl.style.display='block';
  }finally{
    btn.textContent='Accéder au back-office →';btn.disabled=false;
  }
}

async function jbLogin(){
  var code=document.getElementById('jb-code').value.toUpperCase().trim();
  if(!code)return;
  var errEl=document.getElementById('jb-err');
  var btn=document.getElementById('lc-btn-client');
  errEl.style.display='none';
  btn.textContent='Chargement...';btn.disabled=true;
  try{
    var data=await sg('jb_carnets','code_acces=eq.'+code);
    if(!data||!data.length){
      errEl.style.display='block';
      errEl.textContent='Code introuvable — vérifiez le code sur votre devis';
      return;
    }
    CARNET=data[0];
    if(CARNET.devis_id){
      try{var dd=await sg('jb_devis','id=eq.'+CARNET.devis_id);if(dd&&dd[0])DEVIS=dd[0];}catch(e){}
    }
    showPage('pg-client');
    jbRenderClient();
  }catch(e){
    errEl.style.display='block';
    errEl.textContent='Connexion impossible — réessayez dans quelques secondes';
    errEl.style.cursor='pointer';
    errEl.onclick=function(){errEl.style.display='none';jbLogin();};
  }finally{
    btn.textContent='Accéder à mon carnet →';btn.disabled=false;
  }
}

function showPage(id){
  // Masquer la page login (overlay fixe)
  var login=document.getElementById('pg-login');
  if(login){login.style.display='none';}
  // Afficher la bonne page
  document.querySelectorAll('.page').forEach(function(p){p.classList.remove('active');});
  var target=document.getElementById(id);
  if(target){target.classList.add('active');}
  window.scrollTo(0,0);
}

function switchToClient(){showPage('pg-client');jbRenderClient();}

/* ═══════════════════════════════
   VUE CLIENTE
═══════════════════════════════ */
function jbRenderClient(){
  if(!CARNET)return;
  var k=CARNET;
  document.getElementById('cl-name').textContent=k.bride_nom||'';
  var dEl=document.getElementById('cl-dates');dEl.innerHTML='';
  if(k.date_mariage) dEl.innerHTML+='<div class="hero-date-pill"><strong>Mariage</strong> — '+fd(k.date_mariage)+'</div>';
  if(k.date_livraison) dEl.innerHTML+='<div class="hero-date-pill"><strong>Livraison de la robe</strong> — '+fd(k.date_livraison)+'</div>';
  if(k.date_mariage){
    var d=new Date(k.date_mariage),diff=Math.floor((d-new Date())/86400000);
    if(diff>0) document.getElementById('cl-count').innerHTML='<div class="ci"><div class="ci-num">'+diff+'</div><div class="ci-unit">jours</div></div><div style="font-family:Cormorant Garamond,serif;font-size:26px;color:var(--go);padding-top:6px;opacity:.3">·</div><div class="ci"><div class="ci-num">'+Math.floor(diff/7)+'</div><div class="ci-unit">semaines</div></div>';
  }
  if(k.modele_nom) document.getElementById('cl-model').textContent='Modèle « '+k.modele_nom+' »';
  if(k.phrase_signature) document.getElementById('cl-sig').textContent=k.phrase_signature;
  clRenderPlanning();clRenderMesures();clRenderEssayages();clRenderJournal();clRenderChecklist();clRenderPaiements();clRenderDocs();
}

function clSection(name,btn){
  document.querySelectorAll('.cl-sec').forEach(function(s){s.classList.remove('active');});
  document.querySelectorAll('#cl-nav .nav-btn').forEach(function(b){b.classList.remove('active');});
  var s=document.getElementById('cl-sec-'+name);if(s)s.classList.add('active');
  if(btn)btn.classList.add('active');
  window.scrollTo(0,0);
}

function clRenderPlanning(){
  var c=document.getElementById('cl-planning'),plan=(CARNET.planning)||[];
  if(!c)return;
  if(!plan.length){c.innerHTML='<p style="font-size:14px;color:var(--mu);padding:16px 0;font-style:italic">Le planning sera mis à jour par l\'atelier.</p>';return;}
  var fn=false;c.innerHTML='';
  plan.forEach(function(s){
    var done=s.statut==='realise',isN=!done&&!fn;if(isN)fn=true;
    var dot=done?'done':isN?'next':'future';
    var badge=done?'<span class="plan-badge pb-done">Réalisé</span>':isN?'<span class="plan-badge pb-next">Prochain RDV</span>':'<span class="plan-badge pb-future">À venir</span>';
    c.innerHTML+='<div class="plan-item"><div class="plan-dot '+dot+'"></div><div style="flex:1"><div class="plan-title">'+esc(s.titre||'')+'</div>'+(s.date?'<div class="plan-date">'+fd(s.date)+'</div>':'')+'</div>'+badge+'</div>';
  });
}

function clRenderMesures(){
  var c=document.getElementById('cl-mesures'),m=CARNET.mesures||{};if(!c)return;
  var html='<div class="sl">Mesures au corps</div>';var hasAny=false;
  MG.forEach(function(grp){
    var filled=grp.fields.filter(function(f){return m[f[0]]&&m[f[0]]!=='';});
    if(!filled.length)return;hasAny=true;
    html+='<div class="mes-group"><div class="mes-group-title">'+grp.label+'</div><div class="mes-grid-view">';
    filled.forEach(function(f){html+='<div class="mes-card"><div class="mes-lbl">'+f[1]+'</div><div class="mes-val">'+esc(m[f[0]])+' cm</div></div>';});
    html+='</div></div>';
  });
  if(!hasAny) html+='<p style="font-size:14px;color:var(--mu);font-style:italic;padding:8px 0">Les mesures seront renseignées lors du premier essayage.</p>';
  if(m.notes) html+='<div class="mes-notes-box"><div class="mes-group-title" style="margin-bottom:6px">Notes</div>'+esc(m.notes)+'</div>';
  c.innerHTML=html;
}

function clRenderEssayages(){
  var essa=CARNET.essayages||[],el=document.getElementById('cl-essayages'),ee=document.getElementById('cl-essa-empty');
  if(!el)return;
  if(!essa.length){el.innerHTML='';if(ee)ee.style.display='block';return;}
  if(ee)ee.style.display='none';el.innerHTML='';
  essa.forEach(function(e,i){
    var d=document.createElement('div');d.className='essa-item';
    d.innerHTML='<div class="essa-hd"><div class="essa-hd-left"><span class="essa-date-lbl">'+(e.date?fd(e.date):'')+'</span><span class="essa-num">'+esc(e.num||('Essayage '+(i+1)))+'</span></div></div>'+
      '<div class="essa-body">'+esc(e.notes||'')+'</div>'+
      (e.photo_url?'<div class="essa-photos"><img src="'+esc(e.photo_url)+'" class="essa-photo" alt="essayage"></div>':'');
    el.appendChild(d);
  });
}

function clRenderJournal(){
  var j=CARNET.journal||[],g=document.getElementById('cl-journal'),je=document.getElementById('cl-journal-empty');
  if(!g)return;
  if(!j.length){g.innerHTML='';if(je)je.style.display='block';return;}
  if(je)je.style.display='none';g.innerHTML='';
  j.forEach(function(e){
    var d=document.createElement('div');d.className='journal-item';
    d.innerHTML=(e.photo_url?'<img src="'+esc(e.photo_url)+'" class="journal-img" alt="atelier">':'')+
      '<div class="journal-body"><div class="journal-meta">'+(e.date?fd(e.date):'')+'</div><div class="journal-title">'+esc(e.titre||'')+'</div><div class="journal-text">'+esc(e.texte||'')+'</div></div>';
    g.appendChild(d);
  });
}

function clRenderChecklist(){
  var c=document.getElementById('cl-checklist');if(!c)return;
  var cl=CARNET.checklist||null;

  /* Construction initiale depuis DC */
  if(!cl||!cl.length){
    var flat=[];DC.forEach(function(cat){cat.items.forEach(function(item){flat.push({cat:cat.cat,texte:item.texte,delai:item.delai||'',fait:item.fait||false});});});
    CARNET.checklist=flat;sp('jb_carnets','id=eq.'+CARNET.id,{checklist:flat}).catch(function(){});cl=flat;
  } else {
    /* Synchronisation : ajouter les tâches de DC manquantes dans le carnet */
    var existingTextes={};cl.forEach(function(i){existingTextes[i.texte]=true;});
    var added=false;
    DC.forEach(function(cat){
      cat.items.forEach(function(item){
        if(!existingTextes[item.texte]){
          cl.push({cat:cat.cat,texte:item.texte,delai:item.delai||'',fait:false});
          added=true;
        }
      });
    });
    if(added){
      CARNET.checklist=cl;
      sp('jb_carnets','id=eq.'+CARNET.id,{checklist:cl}).catch(function(){});
    }
  }

  var cats={};cl.forEach(function(item,idx){var cat=item.cat||'Divers';if(!cats[cat])cats[cat]=[];cats[cat].push({item:item,idx:idx});});
  c.innerHTML='';
  /* Affichage dans l'ordre de DC */
  var orderedCats=[];
  DC.forEach(function(dcCat){if(cats[dcCat.cat])orderedCats.push(dcCat.cat);});
  Object.keys(cats).forEach(function(k){if(orderedCats.indexOf(k)===-1)orderedCats.push(k);});
  orderedCats.forEach(function(catName){
    if(catName==='Divers'||catName.indexOf('Joséphine Bono')!==-1)return;
    if(catName==='Divers'||catName.indexOf('Joséphine Bono')!==-1)return;
    var wrap=document.createElement('div');wrap.className='check-cat';
    wrap.innerHTML='<div class="check-cat-title">'+esc(catName)+'</div><div class="check-list"></div>';
    c.appendChild(wrap);
    var lst=wrap.querySelector('.check-list');
    cats[catName].forEach(function(o){
      var item=o.item,idx=o.idx;
      var d=document.createElement('div');
      d.className='check-item'+(item.fait?' done':'');
      d.onclick=function(){clToggleCheck(idx,d);};
      var lk=item.lien?'<a href="'+esc(item.lien)+'" target="_blank" onclick="event.stopPropagation()" style="font-size:10px;color:var(--go);text-decoration:none;margin-left:6px;flex-shrink:0">→ article</a>':'';
      d.innerHTML='<div class="check-box">'+(item.fait?'✓':'')+'</div><span class="check-name">'+esc(item.texte)+'</span>'+lk+(item.delai&&item.delai!=='0'?'<span class="check-delay">J&#8209;'+item.delai+'</span>':'');
      lst.appendChild(d);
    });
  });
}
async function clToggleCheck(idx,div){
  var list=CARNET.checklist||[];if(!list[idx])return;
  list[idx].fait=!list[idx].fait;
  div.classList.toggle('done',list[idx].fait);
  div.querySelector('.check-box').textContent=list[idx].fait?'✓':'';
  div.querySelector('.check-name').style.textDecoration=list[idx].fait?'line-through':'';
  try{await sp('jb_carnets','id=eq.'+CARNET.id,{checklist:list});}catch(e){}
}

function clRenderPaiements(){
  var cal=DEVIS&&DEVIS.calendrier_paiements||[],r=document.getElementById('cl-pay-rows'),pa=document.getElementById('cl-pay-btn');
  if(!r)return;r.innerHTML='';var hu=false;
  cal.forEach(function(p){
    var st=p.statut||'a_venir',bc=st==='regle'?'b-paid':st==='en_attente'?'b-pend':'b-fut',bl=st==='regle'?'Réglé':st==='en_attente'?'En attente':'À venir';
    if(st!=='regle')hu=true;
    r.innerHTML+='<div class="pay-row"><span style="font-size:12px;color:var(--mu)">'+esc(p.type||'')+'</span><span style="font-size:12px">'+esc(p.echeance||p.label||'')+'</span><span class="pay-amount">'+fmt(p.montant||0)+' HT</span><span class="badge '+bc+'">'+bl+'</span></div>';
  });
  if(!cal.length)r.innerHTML='<div style="padding:18px;font-size:14px;color:var(--mu);text-align:center">Informations non disponibles.</div>';
  if(pa)pa.style.display=hu?'block':'none';
}

function clRenderDocs(){
  var docs=CARNET.documents||[],c=document.getElementById('cl-docs'),e=document.getElementById('cl-docs-empty');
  if(!c)return;
  if(!docs.length){c.innerHTML='';if(e)e.style.display='block';return;}
  if(e)e.style.display='none';c.innerHTML='';
  var icons={devis:'📄',facture:'🧾',autre:'📎'};
  docs.forEach(function(d){
    var div=document.createElement('div');div.className='doc-item';
    div.innerHTML='<div class="doc-icon">'+(icons[d.type]||'📎')+'</div><div style="flex:1"><div class="doc-name">'+esc(d.nom||'Document')+'</div><div class="doc-type">'+(d.type==='devis'?'Devis':'Facture')+(d.date?' — '+fd(d.date):'')+'</div></div>'+
      (d.url?'<a href="'+esc(d.url)+'" target="_blank" class="doc-dl">Télécharger</a>':'');
    c.appendChild(div);
  });
}

/* ═══════════════════════════════
   BACK-OFFICE ADMIN
═══════════════════════════════ */
function jbRenderAdmin(){
  if(!CARNET)return;
  var bar=document.getElementById('bo-bride-label'),code=document.getElementById('bo-code-label');
  if(bar)bar.innerHTML='Carnet de <strong>'+esc(CARNET.bride_nom||'—')+'</strong>';
  if(code)code.textContent='Code : '+(CARNET.code_acces||'');
  jbFillInfos();
  jbRenderAdminPlanning();
  jbRenderAdminMesures();
  jbRenderAdminEssayages();
  jbRenderAdminJournal();
  jbRenderAdminPay();
  jbRenderAdminDocs();
}

function boSection(name,btn){
  document.querySelectorAll('.bo-sec').forEach(function(s){s.classList.remove('active');});
  document.querySelectorAll('#pg-admin .nav-btn').forEach(function(b){b.classList.remove('active');});
  var s=document.getElementById('bo-sec-'+name);if(s)s.classList.add('active');
  if(btn)btn.classList.add('active');
  window.scrollTo(0,0);
}

/* INFOS */
function jbFillInfos(){
  setv('b-nom',CARNET.bride_nom||'');setv('b-modele',CARNET.modele_nom||'');
  setv('b-sig',CARNET.phrase_signature||'');setv('b-mariage',CARNET.date_mariage||'');setv('b-livraison',CARNET.date_livraison||'');
}
async function jbSaveInfos(){
  var u={bride_nom:val('b-nom'),modele_nom:val('b-modele'),phrase_signature:val('b-sig'),date_mariage:val('b-mariage')||null,date_livraison:val('b-livraison')||null};
  try{await sp('jb_carnets','id=eq.'+CARNET.id,u);Object.assign(CARNET,u);showSaved('infos-saved');jbToast('Infos enregistrées ✓');}
  catch(e){jbToast('Erreur : '+e.message);}
}

/* PLANNING */
function jbRenderAdminPlanning(){
  var c=document.getElementById('bo-plan-rows'),plan=CARNET.planning||[];if(!c)return;c.innerHTML='';
  plan.forEach(function(s,i){
    var so=['a_venir','confirme','realise'].map(function(v){var l={a_venir:'À venir',confirme:'Confirmé',realise:'Réalisé'};return'<option value="'+v+'"'+(s.statut===v?' selected':'')+'>'+l[v]+'</option>';}).join('');
    c.innerHTML+='<div class="plan-edit-row"><input type="text" value="'+esc(s.titre||'')+'" id="pt-'+i+'" placeholder="Intitulé de l\'étape"><input type="date" value="'+(s.date||'')+'" id="pd-'+i+'"><select id="ps-'+i+'">'+so+'</select><button class="btn-danger" onclick="jbRemovePlan('+i+')">×</button></div>';
  });
}
function jbAddPlanRow(){var p=CARNET.planning||[];p.push({titre:'',date:'',statut:'a_venir'});CARNET.planning=p;jbRenderAdminPlanning();}
function jbRemovePlan(i){(CARNET.planning||[]).splice(i,1);jbRenderAdminPlanning();}
async function jbSavePlanning(){
  var plan=(CARNET.planning||[]).map(function(s,i){return{titre:val('pt-'+i)||s.titre,date:val('pd-'+i)||s.date,statut:val('ps-'+i)||s.statut};});
  try{await sp('jb_carnets','id=eq.'+CARNET.id,{planning:plan});CARNET.planning=plan;clRenderPlanning();showSaved('plan-saved');jbToast('Planning enregistré ✓');}
  catch(e){jbToast('Erreur : '+e.message);}
}

/* MESURES */
var MG=[
  {label:'Stature',fields:[['stature','Stature']]},
  {label:'Buste',fields:[
    ['b_poitrine','Tour de poitrine'],['b_dessus_poitrine','Tour dessus poitrine'],['b_dessous_poitrine','Tour dessous poitrine'],
    ['b_taille','Tour de taille'],['b_hanches','Tour de hanches'],['b_pos_hanches','Long. position hanches'],
    ['b_bassin','Tour de bassin'],['b_pos_bassin','Long. position bassin'],['b_cou','Tour de cou'],
    ['b_carrure_devant','Carrure devant'],['b_enc_milieu','Lg encolure milieu devant'],
    ['b_epaule','Long. épaule'],['b_pointe_enc_sein','Pointe enc. — pointe sein'],
    ['b_sein_taille','Pointe sein — haut. taille'],['b_ecart_sein','Écartement pointe sein'],
    ['b_aisselle_taille','Lg aisselle — taille'],['b_carrure_dos','Carrure dos'],['b_montant_dos','Montant dos'],
    ['b_7c_taille_dos','Lg 7ème cerv. — taille dos'],['b_taille_dos_sol','Haut. taille dos — sol'],
    ['b_taille_cote_sol','Lg taille côté — sol'],['b_7c_sol','Haut. 7ème cerv. — sol'],['b_perineal','Montant périnéal']
  ]},
  {label:'Bras droit',fields:[['brd_aisselle','Tour bras aisselle'],['brd_biceps','Tour biceps'],['brd_coude','Tour coude'],['brd_poignet','Tour poignet'],['brd_ep_coude','Long. épaule — coude'],['brd_bras','Long. de bras']]},
  {label:'Bras gauche',fields:[['brg_aisselle','Tour bras aisselle'],['brg_biceps','Tour biceps'],['brg_coude','Tour coude'],['brg_poignet','Tour poignet'],['brg_ep_coude','Long. épaule — coude'],['brg_bras','Long. de bras']]},
  {label:'Jambe',fields:[
    ['j_cuisse','Tour cuisse'],['j_genoux','Tour genoux'],['j_mollet','Tour mollet'],['j_cheville','Tour cheville'],
    ['j_entrejambe','Haut. entrejambe'],['j_taille_genoux','Haut. taille — genoux'],['j_genoux_sol','Haut. genoux — sol'],
    ['j_enfour_devant','Enfourchure devant'],['j_enfour_dos','Enfourchure dos'],['j_jupe_taille','Lg jupe / taille']
  ]},
  {label:'Tête',fields:[['te_tete','Tour de tête'],['te_haut_7c','Haut. tête — 7ème cerv.']]}
];

function jbRenderAdminMesures(){
  var c=document.getElementById('bo-mes-groups'),m=CARNET.mesures||{};if(!c)return;c.innerHTML='';
  MG.forEach(function(grp){
    var d=document.createElement('div');d.className='card';d.style.marginBottom='14px';
    d.innerHTML='<div class="card-title">'+grp.label+'</div><div class="mes-grid-form">';
    grp.fields.forEach(function(f){d.innerHTML+='<div class="field"><label>'+f[1]+'</label><input type="text" id="m-'+f[0]+'" value="'+esc(m[f[0]]||'')+'" placeholder="cm"></div>';});
    d.innerHTML+='</div>';c.appendChild(d);
  });
  setv('b-mes-notes',m.notes||'');
}
async function jbSaveMesures(){
  var m={};MG.forEach(function(grp){grp.fields.forEach(function(f){var el=document.getElementById('m-'+f[0]);if(el)m[f[0]]=el.value;});});
  m.notes=val('b-mes-notes');
  try{await sp('jb_carnets','id=eq.'+CARNET.id,{mesures:m});CARNET.mesures=m;clRenderMesures();showSaved('mes-saved');jbToast('Mesures enregistrées ✓');}
  catch(e){jbToast('Erreur : '+e.message);}
}

/* ESSAYAGES */
function jbRenderAdminEssayages(){
  var essa=CARNET.essayages||[],c=document.getElementById('bo-essa-list'),e=document.getElementById('bo-essa-empty');
  if(!c)return;c.innerHTML='';
  if(!essa.length){if(e)e.style.display='block';return;}
  if(e)e.style.display='none';
  essa.forEach(function(es,i){
    var d=document.createElement('div');d.className='essa-item';
    d.innerHTML='<div class="essa-hd"><div class="essa-hd-left"><span class="essa-date-lbl">'+(es.date?fd(es.date):'')+'</span><span class="essa-num">'+esc(es.num||('Essayage '+(i+1)))+'</span></div><button class="btn-danger" onclick="jbDeleteEssayage('+i+')">Supprimer</button></div>'+
      '<div class="essa-body">'+esc(es.notes||'')+'</div>'+
      (es.photo_url?'<div class="essa-photos"><img src="'+esc(es.photo_url)+'" class="essa-photo" alt="essayage"></div>':'');
    c.appendChild(d);
  });
}
async function jbSaveEssayage(){
  var e={date:val('b-ess-date'),num:val('b-ess-num'),notes:val('b-ess-notes'),photo_url:val('b-ess-photo')};
  if(!e.notes&&!e.date){jbToast('Renseignez au moins la date ou le compte-rendu');return;}
  var essa=CARNET.essayages||[];essa.push(e);
  try{
    await sp('jb_carnets','id=eq.'+CARNET.id,{essayages:essa});CARNET.essayages=essa;
    jbRenderAdminEssayages();clRenderEssayages();
    setv('b-ess-date',new Date().toISOString().split('T')[0]);setv('b-ess-num','');setv('b-ess-notes','');setv('b-ess-photo','');
    jbToast('Essayage enregistré ✓');
  }catch(e){jbToast('Erreur : '+e.message);}
}
async function jbDeleteEssayage(i){
  var essa=CARNET.essayages||[];essa.splice(i,1);
  try{await sp('jb_carnets','id=eq.'+CARNET.id,{essayages:essa});CARNET.essayages=essa;jbRenderAdminEssayages();clRenderEssayages();jbToast('Supprimé');}
  catch(e){jbToast('Erreur : '+e.message);}
}

/* JOURNAL */
function jbRenderAdminJournal(){
  var j=CARNET.journal||[],c=document.getElementById('bo-journal-list');if(!c)return;c.innerHTML='';
  j.forEach(function(e,i){
    var d=document.createElement('div');d.className='journal-item';
    d.innerHTML=(e.photo_url?'<img src="'+esc(e.photo_url)+'" class="journal-img" alt="atelier">':'')+
      '<div class="journal-body"><div class="journal-meta">'+(e.date?fd(e.date):'')+' — <button class="btn-danger btn-sm" onclick="jbDeleteJournal('+i+')" style="margin-left:8px">Supprimer</button></div><div class="journal-title">'+esc(e.titre||'')+'</div><div class="journal-text">'+esc(e.texte||'')+'</div></div>';
    c.appendChild(d);
  });
}
async function jbSaveJournal(){
  var e={date:val('b-jrn-date'),titre:val('b-jrn-titre'),texte:val('b-jrn-texte'),photo_url:val('b-jrn-photo')};
  if(!e.titre){jbToast('Renseignez un titre');return;}
  var j=CARNET.journal||[];j.unshift(e);
  try{
    await sp('jb_carnets','id=eq.'+CARNET.id,{journal:j});CARNET.journal=j;
    jbRenderAdminJournal();clRenderJournal();
    setv('b-jrn-date',new Date().toISOString().split('T')[0]);setv('b-jrn-titre','');setv('b-jrn-texte','');setv('b-jrn-photo','');
    jbToast('Entrée publiée ✓');
  }catch(e){jbToast('Erreur : '+e.message);}
}
async function jbDeleteJournal(i){
  var j=CARNET.journal||[];j.splice(i,1);
  try{await sp('jb_carnets','id=eq.'+CARNET.id,{journal:j});CARNET.journal=j;jbRenderAdminJournal();clRenderJournal();jbToast('Supprimé');}
  catch(e){jbToast('Erreur : '+e.message);}
}

/* PAIEMENTS */
function jbRenderAdminPay(){
  var c=document.getElementById('bo-pay-rows');if(!c||!DEVIS)return;
  var cal=DEVIS.calendrier_paiements||[];
  if(!cal.length){c.innerHTML='<p style="font-size:13px;color:var(--mu);font-style:italic">Aucun calendrier de paiement associé.</p>';return;}
  c.innerHTML='';
  cal.forEach(function(p,i){
    var opts=['a_venir','en_attente','regle'].map(function(s){var l={a_venir:'À venir',en_attente:'En attente',regle:'Réglé'};return'<option value="'+s+'"'+(p.statut===s?' selected':'')+'>'+l[s]+'</option>';}).join('');
    c.innerHTML+='<div style="display:grid;grid-template-columns:1.5fr 1fr 1fr;gap:12px;margin-bottom:8px;align-items:center;padding:11px 14px;background:var(--iv);border:.5px solid var(--br)"><span style="font-size:13px">'+esc(p.type||'')+(p.echeance?' — <span style="color:var(--mu);font-size:11px">'+esc(p.echeance)+'</span>':'')+'</span><span style="font-family:Cormorant Garamond,serif;font-size:16px">'+fmt(p.montant||0)+' HT</span><select data-pay-idx="'+i+'" style="border:.5px solid var(--bs);padding:9px;font-family:Jost,sans-serif;font-size:12px">'+opts+'</select></div>';
  });
}
async function jbSavePaiements(){
  if(!DEVIS)return;
  var cal=DEVIS.calendrier_paiements||[];
  document.querySelectorAll('[data-pay-idx]').forEach(function(s){var i=parseInt(s.getAttribute('data-pay-idx'));if(cal[i])cal[i].statut=s.value;});
  try{await sp('jb_devis','id=eq.'+DEVIS.id,{calendrier_paiements:cal});DEVIS.calendrier_paiements=cal;clRenderPaiements();jbRenderAdminPay();showSaved('pay-saved');jbToast('Statuts mis à jour ✓');}
  catch(e){jbToast('Erreur : '+e.message);}
}

/* DOCUMENTS */
function jbRenderAdminDocs(){
  var docs=CARNET.documents||[],c=document.getElementById('bo-docs-list'),e=document.getElementById('bo-docs-empty');
  if(!c)return;c.innerHTML='';
  if(!docs.length){if(e)e.style.display='block';return;}
  if(e)e.style.display='none';
  var icons={devis:'📄',facture:'🧾',autre:'📎'};
  docs.forEach(function(d,i){
    var div=document.createElement('div');div.className='doc-item';
    div.innerHTML='<div class="doc-icon">'+(icons[d.type]||'📎')+'</div><div style="flex:1"><div class="doc-name">'+esc(d.nom||'Document')+'</div><div class="doc-type">'+(d.type==='devis'?'Devis':'Facture')+(d.date?' — '+fd(d.date):'')+'</div></div>'+
      (d.url?'<a href="'+esc(d.url)+'" target="_blank" class="doc-dl" style="margin-right:10px">Voir</a>':'')+'<button class="btn-danger" onclick="jbDeleteDoc('+i+')">Supprimer</button>';
    c.appendChild(div);
  });
}
async function jbSaveDoc(){
  var d={nom:val('b-doc-nom'),type:val('b-doc-type'),url:val('b-doc-url'),date:new Date().toISOString().split('T')[0]};
  if(!d.nom||!d.url){jbToast('Nom et URL obligatoires');return;}
  var docs=CARNET.documents||[];docs.push(d);
  try{
    await sp('jb_carnets','id=eq.'+CARNET.id,{documents:docs});CARNET.documents=docs;
    jbRenderAdminDocs();clRenderDocs();setv('b-doc-nom','');setv('b-doc-url','');jbToast('Document ajouté ✓');
  }catch(e){jbToast('Erreur : '+e.message);}
}
async function jbDeleteDoc(i){
  var docs=CARNET.documents||[];docs.splice(i,1);
  try{await sp('jb_carnets','id=eq.'+CARNET.id,{documents:docs});CARNET.documents=docs;jbRenderAdminDocs();clRenderDocs();jbToast('Supprimé');}
  catch(e){jbToast('Erreur : '+e.message);}
}

/* ─── CHECKLIST DATA ─── */
var DC=[

  {cat:"1. Vision & cadre du mariage",items:[
    {texte:"Définir la date du mariage",delai:"548"},
    {texte:"Définir le budget global et sa répartition",delai:"540"},
    {texte:"Estimer le nombre d'invités",delai:"540"},
    {texte:"Définir le style du mariage (chic, bohème, minimaliste, champêtre...)",delai:"530"},
    {texte:"Créer un moodboard d'inspiration (Pinterest, Instagram, magazines)",delai:"520"},
    {texte:"Définir vos priorités : lieu, ambiance, expérience invités, gastronomie...",delai:"510"},
    {texte:"Déposer le dossier de mariage civil à la mairie",delai:"500"},
    {texte:"Ouvrir une liste de mariage ou une cagnotte voyage de noces",delai:"490"}
  ]},
  {cat:"2. Lieux & logistique",items:[
    {texte:"Réserver le lieu de réception — visiter plusieurs domaines, châteaux, mas",delai:"480"},
    {texte:"Réserver le lieu de cérémonie (mairie, église, cérémonie laïque)",delai:"470"},
    {texte:"Prévoir un plan B en cas de météo défavorable",delai:"460"},
    {texte:"Organiser les espaces : cocktail, dîner, piste de danse, espace lounge",delai:"450"},
    {texte:"Vérifier l'accès, le stationnement et les contraintes du lieu",delai:"440"},
    {texte:"Vérifier les horaires autorisés (fin de soirée, nuisances sonores)",delai:"430"}
  ]},
  {cat:"3. Invités & expérience",items:[
    {texte:"Établir la liste définitive des invités",delai:"420"},
    {texte:"Envoyer les Save the Date (cartes ou email)",delai:"390"},
    {texte:"Créer un site internet du mariage (Zola, Withjoy, Joy...)",delai:"380"},
    {texte:"Rédiger et envoyer les faire-part",delai:"300"},
    {texte:"Suivre les réponses RSVP",delai:"270"},
    {texte:"Organiser les hébergements pour les invités venant de loin",delai:"260"},
    {texte:"Prévoir le transport des invités (navettes, informations GPS)",delai:"250"},
    {texte:"Créer le plan de table",delai:"120"},
    {texte:"Choisir et commander les cadeaux invités (dragées, attentions...)",delai:"90"}
  ]},
  {cat:"4. Prestataires principaux",items:[
    {texte:"Réserver le photographe — les meilleurs partent 18 mois à l'avance",delai:"450"},
    {texte:"Réserver le vidéaste si souhaité",delai:"440"},
    {texte:"Réserver le traiteur — plusieurs devis et dégustations",delai:"430"},
    {texte:"Définir le menu : cocktail, dîner, brunch du lendemain",delai:"200"},
    {texte:"Gérer les régimes alimentaires spécifiques (allergies, végétarien...)",delai:"130"},
    {texte:"Réserver le DJ ou le groupe de musique live",delai:"420"},
    {texte:"Définir les moments clés avec le DJ : entrée, premier dance, bouquet",delai:"90"},
    {texte:"Réserver le fleuriste — demander une proposition florale complète",delai:"360"},
    {texte:"Valider les compositions florales (bouquet, arche, centres de table)",delai:"90"},
    {texte:"Réserver la coiffeuse pour le jour J",delai:"220"},
    {texte:"Réserver la maquilleuse pour le jour J",delai:"215"},
    {texte:"Planifier les essais coiffure et maquillage — idéalement avec la robe",delai:"150"}
  ]},
  {cat:"5. Décoration & univers visuel",items:[
    {texte:"Définir l'ambiance générale et la palette de couleurs",delai:"400"},
    {texte:"Définir les éléments de décoration : tables, cérémonie, extérieur",delai:"300"},
    {texte:"Réserver ou acheter la décoration — louer si possible",delai:"200"},
    {texte:"Prévoir la scénographie : arche, allée, fond de cérémonie",delai:"180"},
    {texte:"Créer la papeterie : menus, plans de table, signalétique, programmes",delai:"120"},
    {texte:"Prévoir un livre d'or ou une alternative créative (polaroids, ardoise...)",delai:"90"}
  ]},
  {cat:"6. Animations & expérience invités",items:[
    {texte:"Prévoir les animations : photobooth, jeux, animation musicale",delai:"200"},
    {texte:"Organiser les surprises : discours, vidéos, slideshow",delai:"120"},
    {texte:"Prévoir une animation dédiée aux enfants",delai:"180"},
    {texte:"Réserver une garde d'enfants (baby-sitter ou agence spécialisée)",delai:"180"},
    {texte:"Aménager un espace confortable dédié aux enfants",delai:"90"},
    {texte:"Planifier l'enterrement de vie de jeune fille (EVJF)",delai:"195"}
  ]},
  {cat:"7. Transport & déplacements",items:[
    {texte:"Organiser le transport des mariés entre les lieux",delai:"200"},
    {texte:"Prévoir les déplacements cérémonie vers réception pour les invités",delai:"180"},
    {texte:"Informer les invités des accès et options d'hébergement à proximité",delai:"150"},
    {texte:"Vérifier le retour des invités le soir (taxis, navettes, covoiturage)",delai:"60"}
  ]},
  {cat:"8. Coordination & organisation",items:[
    {texte:"Créer le planning détaillé du jour J (horaire par horaire)",delai:"60"},
    {texte:"Définir le déroulé de la cérémonie avec l'officiant",delai:"90"},
    {texte:"Répartir les rôles entre témoins et proches (accueil, coordination)",delai:"60"},
    {texte:"Désigner une personne référente pour le jour J — votre chef d'orchestre",delai:"45"},
    {texte:"Envoyer le planning détaillé à chaque prestataire",delai:"30"},
    {texte:"Confirmer tous les prestataires une dernière fois",delai:"14"},
    {texte:"Vérifier tous les horaires et adresses exacts",delai:"7"},
    {texte:"Préparer les enveloppes de règlement pour chaque prestataire",delai:"5"}
  ]},
  {cat:"9. Beauté & préparation personnelle",items:[
    {texte:"Mettre en place une routine soin : hydratation, protection solaire, sommeil",delai:"180"},
    {texte:"Planifier les rendez-vous beauté dans les semaines précédant le mariage",delai:"60"},
    {texte:"Réserver manucure et pédicure — couleur en accord avec la robe",delai:"21"},
    {texte:"Prévoir un test autobronzant si souhaité — au moins 2 semaines avant",delai:"21"},
    {texte:"Planifier un moment de détente la veille : spa, massage, bain",delai:"1"},
    {texte:"Apprendre à marcher avec vos chaussures — portez-les à la maison",delai:"30"}
  ]},
  {cat:"10. Logistique personnelle",items:[
    {texte:"Préparer la valise du voyage de noces",delai:"7"},
    {texte:"Préparer la valise ou le sac du lendemain",delai:"5"},
    {texte:"Prévoir une tenue de rechange pour la soirée si souhaité",delai:"60"},
    {texte:"Préparer une trousse de secours : épingles, aiguille-fil, Biafine, pansements, anti-taches",delai:"7"},
    {texte:"Récupérer la robe chez Joséphine Bono",delai:"3"},
    {texte:"Récupérer les alliances chez le joaillier",delai:"3"},
    {texte:"Ranger tous les accessoires, documents et enveloppes prestataires ensemble",delai:"2"}
  ]},
  {cat:"11. Derniers détails",items:[
    {texte:"Finaliser le nombre d'invités et confirmer au traiteur",delai:"21"},
    {texte:"Préparer et répéter les discours",delai:"14"},
    {texte:"Finaliser les playlists avec le DJ",delai:"14"},
    {texte:"Vérifier que tous les paiements prestataires sont à jour",delai:"7"},
    {texte:"Imprimer les plans de table et marque-places",delai:"5"},
    {texte:"Charger tous les appareils photo et téléphones",delai:"1"}
  ]},
  {cat:"12. Veille & jour J",items:[
    {texte:"Préparer tous les éléments nécessaires la veille",delai:"1"},
    {texte:"Vérifier les accessoires : voile, bijoux, chaussures, jarretière",delai:"1"},
    {texte:"Se coucher tôt — le repos est le meilleur soin beauté",delai:"1"},
    {texte:"Manger correctement le matin du mariage",delai:"0"},
    {texte:"Lâcher prise — tout est prêt, faites confiance à vos prestataires",delai:"0"},
    {texte:"Profiter pleinement de chaque instant de cette journée unique",delai:"0"}
  ]}
];

/* ─── UTILS ─── */
function showSaved(id){var el=document.getElementById(id);if(!el)return;el.style.display='inline';setTimeout(function(){el.style.display='none';},2500);}
function jbToast(msg){var c=document.getElementById('jb-toast');var d=document.createElement('div');d.className='toast-item';d.textContent=msg;c.appendChild(d);setTimeout(function(){if(d.parentNode)d.parentNode.removeChild(d);},3000);}
function fmt(n){return(parseFloat(n)||0).toLocaleString('fr-FR',{minimumFractionDigits:2,maximumFractionDigits:2})+'\u00a0\u20ac';}
function fd(s){if(!s)return'';try{return new Date(s).toLocaleDateString('fr-FR',{day:'numeric',month:'long',year:'numeric'});}catch(e){return s;}}
function val(id){var el=document.getElementById(id);return el?el.value:'';}
function setv(id,v){var el=document.getElementById(id);if(el)el.value=v;}
function esc(s){return String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}

window.addEventListener('DOMContentLoaded',function(){
  var params=new URLSearchParams(window.location.search);
  var code=params.get('code');
  if(code){
    document.getElementById('jb-code').value=code.toUpperCase();
    // Pas d'auto-login : le code est pré-rempli mais l'utilisateur clique lui-même
    // Cela permet à l'atelier de basculer en mode admin si besoin
  }
  var today=new Date().toISOString().split('T')[0];
  ['b-jrn-date','b-ess-date'].forEach(function(id){var el=document.getElementById(id);if(el)el.value=today;});
});
</script>
</body>
</html>
